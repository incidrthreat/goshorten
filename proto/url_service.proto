syntax = "proto3";

option go_package = ".;url_service";

service Shortener {
    rpc GetURL(URLRequest) returns (URLResponse);
}

message URLRequest {
    string url = 1;
}

message URLResponse {
    string shortened = 1;
}