# Populate all values before applying.
# Recommendation: manage this with a secrets tool (e.g. Sealed Secrets, External Secrets, SOPS)
# and never commit real values to git.
apiVersion: v1
kind: Secret
metadata:
  name: goshorten-secrets
  namespace: goshorten
type: Opaque
stringData:
  POSTGRES_USER: "goshorten"
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_DB: "goshorten"
  REDIS_PASSWORD: "changeme"
  JWT_SECRET: "changeme-use-a-long-random-string"
  ADMIN_EMAIL: "admin@goshorten.local"
  ADMIN_PASSWORD: "changeme"
